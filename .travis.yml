---
sudo: required
language: python
services:
  - docker
install:
  - pip install -r test-requirements.txt
script:
  - tox
notifications:
  irc:
    channels:
      - "irc.freenode.org#osism"
    use_notice: true
